<%= f.input :name %>
<%= f.association :unlock %>
<br/>
<h3>Areas</h3>
<%= f.simple_fields_for :areas do |a| %>
  <%= a.input :name %>
  <%= a.association :unlock %>
  <%= a.link_to_remove "Remove this area" %>
  <p></p>
<% end %>
<%= f.link_to_add "Add an area", :areas %>
<p></P>
<%= f.button :submit %>
